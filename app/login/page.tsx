"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function Login() { const [error,setError]=useState(""); const router=useRouter(); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});if(r.ok)router.push("/team");else setError((await r.json()).error||"Could not sign in");} return <main className="container" style={{maxWidth:480,paddingTop:80}}><div className="card"><h1>Team sign in</h1><p className="muted">Use your team credentials to manage the fund ledger.</p><form onSubmit={submit}><label>Email<input name="email" type="email" required /></label><label>Password<input name="password" type="password" required /></label>{error&&<div className="error">{error}</div>}<button className="button">Sign in</button></form></div></main> }
