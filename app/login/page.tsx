"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function Login() { const [error,setError]=useState(""); const router=useRouter(); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});if(r.ok)router.push("/team");else setError((await r.json()).error||"Could not sign in");} return <main className="login-shell"><div className="login-form-area"><div className="card login-card"><a className="brand login-mark" href="/">Superstrika#7046</a><header className="page-heading"><h1>Sign in</h1></header><form onSubmit={submit}><label>Email<input name="email" type="email" autoComplete="email" required /></label><label>Password<input name="password" type="password" autoComplete="current-password" required /></label>{error&&<div className="error">{error}</div>}<button className="button">Sign in</button></form><a className="login-back" href="/fund">← Fund ledger</a></div></div></main> }
